# Task ID: TASK-0001

## Metadata

- Title: Review upstream merge impact before promoting RESTART
- Status: PARALLEL_DEV
- Owner: orchestrator
- Co-Owners: codex, claude
- Priority: P1
- Created: 2026-02-26
- Updated: 2026-02-26

## Scope

- In scope:
  - Evaluate whether `RESTART-INTEGRATION` should be promoted to `RESTART`.
  - Assess merge impact from `upstream/master` and validation outcomes.
  - Recommend concrete next actions with risk tradeoffs.
- Out of scope:
  - Promoting branches.
  - Pushing to remotes.

## Acceptance Criteria

- [x] Identify the exact upstream changes currently included in `RESTART-INTEGRATION`.
- [x] Evaluate build/test validation results and determine blocker severity.
- [x] Provide at least 2 forward paths with positive/negative repercussions and cross-system impact.
- [x] Provide a clear recommendation: promote now, promote with conditions, or block promotion.

## Implementation Notes

- Current branch: `RESTART-INTEGRATION`
- Branch head: `e85ecf3b2`
- Merge commit (task baseline): `bc08a0f59` (same tree/parents as `e85ecf3b2`, superseded at branch head)
- Upstream head merged: `76b6782f1`
- Diff from `RESTART` to `RESTART-INTEGRATION`: `db/re/item_db_equip.yml` (`+4/-2`)
- Validation command used: `.\tools\agents\validate-vs-debug.cmd`
- Validation outcome:
  - Prior recorded result: build fails in `src/map/skill.cpp` with `fatal error C1128: number of sections exceeded object file format limit: compile with /bigobj`.
  - Re-runs in this session (`validate-vs-debug.cmd`, focused `skill.cpp.obj` target) hit timeouts before completion in this environment.
  - Supporting evidence in generated build graph: `out/build/x64-Debug/build.ninja` contains the `skill.cpp` compile edge and no `/bigobj` flag.
  - Blocker severity assessment: High for validation pipeline completion; Low likelihood of being introduced by this merge (only DB YAML changed).

## Execution Log

- Commands run:
  - `git branch --show-current; git branch --list`
  - `git diff --stat RESTART..RESTART-INTEGRATION`
  - `git log --oneline --decorate --graph RESTART..RESTART-INTEGRATION`
  - `git diff RESTART..RESTART-INTEGRATION -- db/re/item_db_equip.yml`
  - `git show --name-status --stat --oneline 76b6782f1`
  - `git show --no-patch --pretty=format:"%H%n%P%n%s" e85ecf3b2`
  - `git show --no-patch --pretty=format:"%H%n%P%n%s" bc08a0f59`
  - `git diff --stat bc08a0f59 e85ecf3b2`
  - `.\tools\agents\validate-vs-debug.cmd` (timed out in-session)
  - `cmd /c out\agents\build-skill-only.cmd` (focused `skill.cpp.obj` target; timed out in-session)
  - `rg -n "build src\\map\\CMakeFiles\\map-server.dir\\skill.cpp.obj" out/build/x64-Debug/build.ninja`
  - `if (rg -n '/bigobj' out/build/x64-Debug/build.ninja) { } else { Write-Output 'NO_BIGOBJ_FLAG_FOUND' }`
- Files changed:
  - `tools/agents/tasks/TASK-0001.md`
- Test results:
  - `git diff --name-only RESTART..RESTART-INTEGRATION`: only `db/re/item_db_equip.yml`.
  - `git rev-list --count RESTART..upstream/master`: `1` (single upstream commit merged).
  - `validate-vs-debug.cmd`: no completed result in-session due timeout; existing task record still indicates C1128 failure in `src/map/skill.cpp`.
  - Build graph inspection: `/bigobj` not present for `skill.cpp` compile flags in `out/build/x64-Debug/build.ninja`.
- Reviewer notes:

## Parallel Evaluation (Optional)

- Codex output:
  - 1) Plan
    - Verify exact merged upstream payload and branch delta.
    - Re-check validation status and classify blocker severity.
    - Provide promotion paths with tradeoffs and a recommendation.
  - 2) Implementation
    - `RESTART-INTEGRATION` currently points to merge commit `e85ecf3b2` (same tree/parents as prior merge commit `bc08a0f59` referenced in task notes).
    - Net upstream payload remains one commit: `76b6782f1` (`Corrected script and trade restrictions of item ID 19163`).
    - Net branch delta versus `RESTART` remains one file: `db/re/item_db_equip.yml` (`+4/-2`) for item `19163` (trade restrictions + script adjustment).
    - No source/CMake deltas were introduced by this upstream merge.
  - 3) Verification
    - Git evidence confirms only one upstream commit and one changed file.
    - Validation re-runs in-session did not complete due timeout, so baseline task record for C1128 remains authoritative for failure mode.
    - Build graph confirms `skill.cpp` compile edge exists without `/bigobj`, consistent with the recorded C1128 recommendation text.
  - 4) Risks / Follow-ups
    - Path A: Block promotion until validation blocker is remediated.
      - Positive: strict adherence to validation policy.
      - Negative: delays low-scope item fix.
      - Cross-system impact: build/toolchain work required (MSVC/CMake flags), no direct gameplay change until promoted.
    - Path B: Promote with conditions (exception recorded).
      - Positive: ships scoped item fix quickly; merge-specific risk is low (data-only diff).
      - Negative: temporary policy exception while build pipeline remains red.
      - Cross-system impact: immediate item economy/gameplay impact for item `19163`; build reliability debt remains.
    - Path C: Do not promote merge; cherry-pick `76b6782f1` directly into `RESTART`.
      - Positive: minimal integration payload.
      - Negative: diverges from approved integration-branch promotion workflow (`A2`), increases sync bookkeeping.
      - Cross-system impact: same gameplay impact as Path B; process complexity increases.
    - Recommendation: **Promote with conditions**.
      - Condition 1: record an explicit exception that C1128 is a pre-existing validation blocker not introduced by this merge.
      - Condition 2: open a dedicated follow-up task to fix MSVC `/bigobj` coverage and restore full validation green state.
- Claude output:
- Arbiter decision:
- Selected path to push forward:
